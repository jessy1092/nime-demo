{
  "name": "nime-demo",
  "version": "0.1.0",
  "description": "The simple demo in NIME. It's the case converter.",
  "main": "index.js",
  "scripts": {
    "start": "cross-env UV_THREADPOOL_SIZE=32 DEBUG=nime:* node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Lee <jessy1092@gmail.com>",
  "license": "MIT",
  "engines": {
    "node": ">=6"
  },
  "devDependencies": {
    "cross-env": "^1.0.8",
    "nime": "^0.3.0"
  }
}
